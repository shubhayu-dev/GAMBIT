# engine/
Owner: Member A.
C++ core: board representation, move generation, search (minimax/alpha-beta),
evaluation function, agent implementations, the standardized Agent Interface
(see `../docs/06_agent_interface.md`). Prototype in Python first if that's faster
for the team; port hot paths here once the loop is proven.
