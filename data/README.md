# data/
Bulk data lives here at runtime and is gitignored: SQLite DB (agents,
experiments, benchmarks, runs, configs) + Parquet trajectory files + JSON
configs/metadata. See `../docs/07_dataset_benchmark_spec.md` for the schema.
