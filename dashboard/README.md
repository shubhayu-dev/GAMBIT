# dashboard/
Owner: Member D.

## Week 8 — done
- `report_html.py` — static HTML report generator (Streamlit isn't
  installable in this sandbox — see the module docstring deviation note).
  Renders the capability profile, primary weakness, ranked diagnostic
  evidence, and before/after intervention validation, with every deviation
  from the original spec listed in the footer.

Generated at `../data/gambit_report.html` by `run_weeks_7_8.py` (or
`run_full_pipeline.py` on fresh data). Open it in any browser.
